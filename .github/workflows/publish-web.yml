name: publish-web

on:
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    name: publish

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x

      - name: Inject API URL into appsettings.json
        run: |
          jq --arg url "${{ secrets.CONFLUENCE_CHAT_API_URL }}" '. + {ConfluenceChatApiUrl: $url}' ConfluenceChatRAG/wwwroot/appsettings.json > tmp.json
          mv tmp.json ConfluenceChatRAG/wwwroot/appsettings.json

      - name: Build Blazor WASM
        run: dotnet publish ConfluenceChatRAG/ConfluenceChatRAG.csproj -c Release -o ./published

      - name: Deploy to Azure Static Web App
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.WEB_AZURE_STATIC_WEB_APPS_API_TOKEN }}
          action: "upload"
          app_location: "./published/wwwroot"
          output_location: ""
          skip_app_build: true
          skip_api_build: true
